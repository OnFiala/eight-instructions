# X draft — not posted

Build 002 of 8 Instructions: a city you can reprogram.

Change a rule inside a Brainfuck machine. Moving vans finish their road; new trips use the new code. Unused versions give memory back.

Source, tests and replay files are open.

# Follow-up reply — not posted

BF runs Thread compilation, source storage, module lifetimes, routes and city steps. Python bootstraps the kernel. Generic JS/Wasm executes eight BF instructions. JavaScript renders real BF output with generated art. BF does not draw the graphics.

Publication note: proposed texts only. The release receipt determines whether the described public build has shipped. Nothing is sent to X.
